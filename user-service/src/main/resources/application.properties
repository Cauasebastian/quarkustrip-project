quarkus.application.name=user-service
quarkus.http.port=8087
quarkus.grpc.server.port=9007
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${USER_DB_USER:users}
quarkus.datasource.password=${USER_DB_PASSWORD:users}
quarkus.datasource.jdbc.url=${USER_JDBC_URL:jdbc:postgresql://localhost:5432/users}
quarkus.datasource.reactive.url=${USER_REACTIVE_URL:postgresql://localhost:5432/users}
quarkus.flyway.migrate-at-start=true
quarkus.hibernate-orm.database.generation=validate
kafka.bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
mp.messaging.outgoing.outbox.connector=smallrye-kafka
mp.messaging.outgoing.outbox.value.serializer=org.apache.kafka.common.serialization.StringSerializer
mp.messaging.outgoing.outbox.merge=true
quarkus.otel.exporter.otlp.traces.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}
quarkus.log.console.json.enabled=${JSON_LOGS:false}
